{
  "app": {
    "name": "hbario",
    "version": "0.2.0",
    "description": "Hedera workflows, on demand. hbario is a payment-gated commerce agent that sells one service — Hedera workflow execution — billed in HBAR. Customers (humans in the chat, AI agents over MCP) describe what they need, hbario plans it with Hedera Agent Kit, gates execution behind an HBAR payment, runs each step through the customer's own wallet, and verifies every transfer on the Hedera Mirror Node before issuing a receipt.",
    "interop": {
      "x402_compatible": true,
      "mcp_protocol_version": "2025-03-26",
      "notes": "Inspired by AP2, UCP, ACP, and MPP agent-commerce conventions; exposes machine-readable /agent.json + /api/services manifests for indexing."
    },
    "hosted_url": "https://hbario.com",
    "safety_model": "human_approval_required"
  },
  "workflows": {
    "supported_types": [
      "single_payment",
      "bulk_payout",
      "bulk_account_creation",
      "liquidity_path_analysis",
      "compound"
    ]
  },
  "powered_by": {
    "agent_kit": {
      "name": "hedera-agent-kit",
      "version": "3.8.x",
      "mode": "RETURN_BYTES",
      "description": "Used to plan and emit unsigned Hedera transactions which are signed by the user's wallet (WalletConnect / HIP-820). The server never holds private keys."
    },
    "payments_mcp": {
      "name": "hbario-payments-mcp",
      "version": "0.1.0",
      "transport": "streamable-http",
      "endpoint": "/api/mcp"
    }
  },
  "tools": {
    "supported": [
      {
        "name": "validateAccount",
        "category": "read",
        "description": "Validate a Hedera account ID format."
      },
      {
        "name": "getAccountBalance",
        "category": "read",
        "description": "Get the HBAR balance of a Hedera account."
      },
      {
        "name": "estimateTransferFees",
        "category": "estimate",
        "description": "Estimate the network fee for a single HBAR transfer."
      },
      {
        "name": "estimateBulkPayoutFees",
        "category": "estimate",
        "description": "Estimate the total network fee for a bulk payout."
      },
      {
        "name": "prepareHbarTransfer",
        "category": "prepare",
        "description": "Prepare an HBAR transfer instruction for user approval. Does NOT execute."
      },
      {
        "name": "prepareBulkPayout",
        "category": "prepare",
        "description": "Prepare a bulk payout instruction for user approval. Does NOT execute."
      },
      {
        "name": "verifyTransaction",
        "category": "verify",
        "description": "Verify a transaction against expected details."
      },
      {
        "name": "getTransactionReceipt",
        "category": "verify",
        "description": "Get the receipt for a completed transaction."
      }
    ]
  },
  "mcp": {
    "endpoint": "/api/mcp",
    "transport": "streamable-http",
    "protocol_version": "2025-03-26",
    "auth": "bearer-token (per-user API key, see /mcp page)",
    "tools": [
      "list_services",
      "request_workflow",
      "create_payment_order",
      "submit_payment",
      "get_receipt",
      "verify_transaction"
    ]
  },
  "payment": {
    "currency": "HBAR",
    "protocol": "hedera-payments-mcp",
    "x402_compatible": true,
    "unlock_price_hbar": 1
  },
  "api": {
    "endpoints": {
      "agent_manifest": "/agent.json",
      "services_manifest": "/api/services",
      "mcp_endpoint": "/api/mcp",
      "mcp_key": "/api/mcp/key",
      "chat_quote": "/api/chat/quote",
      "chat_agent": "/api/chat/agent",
      "workflows_list": "/api/workflows",
      "workflows_detail": "/api/workflows/[id]",
      "verify_order": "/api/orders/[id]/verify",
      "receipt": "/api/receipts/[id]"
    }
  }
}
